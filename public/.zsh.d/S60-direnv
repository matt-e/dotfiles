if ! command -v direnv > /dev/null; then
      echo "Installing direnv via asdf..."
      asdf plugin-add direnv
      asdf install direnv 2.23.1
      asdf global direnv 2.23.1
else
    eval "$(direnv hook zsh)"
fi
